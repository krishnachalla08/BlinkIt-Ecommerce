server:
  port: 9002

spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
  datasource:
    url: jdbc:mysql://localhost:3307/BlinkitDatabase_dev
    username: admin1815
    password: Blinkit!*!%
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      database-platform: org.hibernate.dialect.MySQLDialect
aws:
  region: eu-north-1
  cdnUrl: https://d21n11a7lmecaa.cloudfront.net